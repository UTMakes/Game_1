// Toolbar.jsx — Tile/node picker, layer toggle
// Provides the main toolbar for selecting tiles, nodes, and switching layers.

export default function Toolbar() {
  // TODO: Implement toolbar with tile/node picker and layer toggle
  return (
    <div className="toolbar">
      <p>Toolbar — Coming Soon</p>
    </div>
  );
}
